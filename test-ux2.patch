--- src/components/layout/profile-dropdown.tsx
+++ src/components/layout/profile-dropdown.tsx
@@ -34,7 +34,7 @@
   return (
     <DropdownMenu>
       <DropdownMenuTrigger asChild>
-        <Button variant="ghost" size="icon" title="Profile">
+        <Button variant="ghost" size="icon" title="Profile" aria-label="Open user profile menu">
           {userImage ? (
             <img
               src={userImage}
